import 'package:flutter/material.dart';

class figma_card extends StatelessWidget {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return Container(
      width: 318,
      height: 1328,
      child: Stack(
        children: <Widget>[
          Positioned(top: 928, left: 0, child: null),
          Positioned(top: 464, left: 0, child: null),
          Positioned(top: 0, left: 0, child: null),
        ],
      ),
    );
  }
}
